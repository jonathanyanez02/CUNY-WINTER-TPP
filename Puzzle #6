//Jonathan Yanez

function reverse(s) {    /*creates function*/
  var o = '';  /*sets var to an empty string*/
  for (var i = s.length - 1; i >= 0; i--)  /*iterates backwards*/
    o += s[i]; /* adds with index */
  return o;   /*returns string*/
}
